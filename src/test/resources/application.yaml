--- #구분
spring:
    mustache:
        suffix: .html
    jpa:
        show-sql: true
        properties:
            strategy: org.hibernate.dialect.MySQL5InnoDBDialect
    session:
        store-type: jdbc
    security:
        oauth2:
            client:
                registration:
                    google:
                        scope: profile,email
                        client-id: test
                        client-secret: test